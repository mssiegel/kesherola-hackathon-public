{
  "name": "storyline",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Storyline — students get an AI phone call from a literary character; teachers get Claude-assessed transcripts. Built on the Dial Node SDK.",
  "scripts": {
    "start": "node --import tsx --env-file-if-exists=.env server.ts",
    "server": "node --import tsx --watch --env-file-if-exists=.env server.ts",
    "web": "npm --prefix web run dev",
    "dev": "concurrently -n server,web -c blue,magenta \"npm run server\" \"npm run web\"",
    "build": "npm --prefix web install && npm --prefix web run build",
    "typecheck": "tsc --noEmit && npm --prefix web run typecheck"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.104.1",
    "@getdial/sdk": "^0.6.3",
    "express": "^5.2.1",
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.9.2",
    "@types/ws": "^8.18.1",
    "concurrently": "^9.1.2",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
